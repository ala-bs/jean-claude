// LEFTOVER SCRATCH FILE — SAFE TO DELETE.
//
// A code-review agent created this to probe picomatch behavior while checking
// the auto-review path matching. Its findings are now covered properly by
// auto-review.test.ts. It is emptied rather than removed because file deletion
// was not available in the session that wrote this; delete the file outright.
import { describe, it } from 'vitest';

describe.skip('leftover scratch file — delete me', () => {
  it('has no tests', () => {});
});
